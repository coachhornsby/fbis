name: ACTION post-deploy repair — PAUSED

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo "ACTION/Apify is paused. No collector calls will run."
