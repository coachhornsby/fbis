name: ACTION daily market snapshot — PAUSED

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo "ACTION/Apify is paused. No paid Actor call will run."
