name: ACTION pro player props — PAUSED

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo "ACTION/Apify is paused. No player-prop collector call will run."
