name: ACTION daily watchdog — PAUSED

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo "ACTION/Apify is paused. No recovery or paid Actor call will run."
